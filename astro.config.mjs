// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://codality.eu',
  // GitHub Pages deployment with custom domain
  output: 'static',
});
